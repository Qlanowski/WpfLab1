<UserControl x:Class="WpfLab1.Controlls.ShowMailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfLab1.Controlls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid DataContext="{Binding Email}" ScrollViewer.CanContentScroll="True" Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="30"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="{Binding Title}" FontSize="25"/>
            <Label Grid.Row="1" Content="{Binding From}" FontSize="20"/>
            <Label Grid.Row="2" Content="{Binding Date}"/>
            <TextBlock Grid.Row="3"  TextWrapping="Wrap" TextAlignment="Justify" Text="{Binding Body}" />
        </Grid>
    </ScrollViewer>
</UserControl>
