<UserControl x:Class="WpfLab1.Controlls.ShowMailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfLab1.Controlls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid DataContext="{Binding Email}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Hi"/>
        <!--<Label Grid.Row="0" Content="{Binding Title}"/>-->
        <Label Grid.Row="1" Content="{Binding Title}"/>
        <Label Grid.Row="2" Content="{Binding Title}"/>
        <Label Grid.Row="3" Content="{Binding Title}"/>
    </Grid>
</UserControl>
